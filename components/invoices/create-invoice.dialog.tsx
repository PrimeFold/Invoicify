import { ClientOption } from '@/types/client';
import { Dialog, DialogContent } from '../ui/dialog';


const CreateInvoiceDialog = ({clientId}:{clientId:string | null}) => {
  return (
    <Dialog>
      <DialogContent>

      </DialogContent>
    </Dialog>
  )
}

export default CreateInvoiceDialog;
